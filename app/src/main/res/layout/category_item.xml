<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_margin="8dp"
    android:id="@+id/itemFull"
    android:layout_height="wrap_content"
    android:background="@drawable/size_back"
    android:padding="16dp">

    <ImageView
        android:id="@+id/imageCategory"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:background="@drawable/image_back"
        android:padding="8dp"
        android:src="@drawable/cat1_original"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/buttonCategory"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:background="@drawable/size_back_default"
        android:text="Cats"
        app:layout_constraintLeft_toLeftOf="@id/imageCategory"
        app:layout_constraintRight_toRightOf="@id/imageCategory"
        app:layout_constraintTop_toBottomOf="@id/imageCategory" />


</androidx.constraintlayout.widget.ConstraintLayout>